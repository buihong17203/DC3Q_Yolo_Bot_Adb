# DC3Q_YOLO_BOT_ADB - Python Dependencies
# Python: 3.11.x

# Computer Vision - Xử lý hình ảnh, ảnh chụp màn hình, nhận diện và so khớp hình ảnh
opencv-python>=4.10.0
numpy>=1.26.0,<3.0.0
Pillow>=10.4.0

# YOLO / Deep Learning - Nhận diện đối tượng bằng YOLO, chạy mô hình AI và xử lý tensor
ultralytics>=8.3.0
torch>=2.4.0
torchvision>=0.19.0

# Configuration - Đọc và quản lý file cấu hình YAML, biến môi trường và file .env
PyYAML>=6.0.2
python-dotenv>=1.0.1

# Data validation - Kiểm tra, chuẩn hóa và xác thực dữ liệu cấu hình, thiết bị, tài khoản và tác vụ
pydantic>=2.9.0

# HTTP / Networking - Gửi request API, giao tiếp mạng bất đồng bộ và kết nối WebSocket
requests>=2.32.0
httpx>=0.27.0
aiohttp>=3.10.0
websockets>=13.0

# System monitoring - Theo dõi CPU, RAM, tiến trình ADB, giả lập và tài nguyên hệ thống
psutil>=6.0.0

# Logging - Ghi log hoạt động, lỗi, cảnh báo và thông tin chạy của hệ thống
loguru>=0.7.2

# Retry / Recovery - Tự động thử lại khi ADB, API, thiết bị hoặc tác vụ gặp lỗi tạm thời
tenacity>=9.0.0

# Scheduling - Lập lịch và chạy các tác vụ định kỳ như kiểm tra thiết bị, tài khoản và worker
APScheduler>=3.10.0

# Database - Làm việc với cơ sở dữ liệu, lưu tài khoản, thiết bị, lịch sử và trạng thái tác vụ
SQLAlchemy>=2.0.0

# Data / Excel - Xử lý dữ liệu dạng bảng và đọc ghi file Excel
pandas>=2.2.0
openpyxl>=3.1.0

# Serialization - Đọc và ghi dữ liệu JSON với tốc độ cao
orjson>=3.10.0

# CLI - Xây dựng giao diện dòng lệnh và hiển thị thông tin terminal dễ đọc
rich>=13.8.0
typer>=0.12.0

# Testing - Viết và chạy kiểm thử cho các module, bao gồm cả chức năng bất đồng bộ
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Code quality - Kiểm tra lỗi code, chuẩn hóa phong cách và phát hiện vấn đề trong mã nguồn
ruff>=0.6.0
