name: dc3q_hoat_dong

steps:
  - action: hoat_dong
    account_id: "${account.id}"
    package: "com.daichien.mobile"
    entry_threshold: 0.80
    close_threshold: 0.84
    timeout: 12
    interval: 0.5
